extends layouts/login-layout.pug 

block content 
  .loginContainer 
    h1 Login 
    form(method="POST")

      p.errorMessage #{errorMessage}
    
      //- input(type="text", name="logUsername", placeholder="Benutzername oder Email", value=logUsername, required="")
      //- input(type="password", name="logPassword", placeholder="Passwort", value=logPassword, required="")
      input(type="text", name="logUsername", placeholder="Benutzername oder Email", value="Grobi", required="")
      input(type="password", name="logPassword", placeholder="Passwort", value="geheim", required="")

      input(type="submit", value="Login")
    a(href="/register") Noch nicht registriert? Hier anmelden.
     